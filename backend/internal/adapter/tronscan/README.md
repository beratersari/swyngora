# adapter/tronscan

Public Tronscan `token_trc20` API (no key) for TRC-20 holder counts
(JST, SUN, WIN, and other Tron tokens).

Used after Routescan in the holders cascade.

```bash
cd backend && go test ./internal/adapter/tronscan/
```
