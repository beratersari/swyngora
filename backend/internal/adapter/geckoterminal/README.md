# adapter/geckoterminal

Public `https://api.geckoterminal.com/api/v2` token `/info` for holder counts.
No API key. `/top_holders` is paid/unauthorized and is not called.

Used only as a holders fallback after CoinMarketCap.
