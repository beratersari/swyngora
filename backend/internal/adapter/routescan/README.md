# adapter/routescan

Public Routescan etherscan-compatible API (no key) for EVM token holders,
including Chiliz fan tokens (`chainId` 88888).

Used after CoinMarketCap / Coin Metrics / GeckoTerminal / Ethplorer.

```bash
cd backend && go test ./internal/adapter/routescan/
```
