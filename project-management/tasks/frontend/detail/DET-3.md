# DET-3: Candle chart + interval toolbar (implementation)

| Field | Value |
|---|---|
| **ID** | DET-3 |
| **Epic** | coin-detail-and-indicators |
| **Status** | done |
| **Area** | frontend |
| **Type** | implementation |
| **Blocked by** | DET-A, DET-1, DET-2 |

## Summary

DetailChartToolbar (interval from `/intervals`, bar limit), CandleChartHost with OHLCV from candles API, URL sync for interval/limit, poll + visibility pause. See DET-A.

## Status

**done.** `DetailChartToolbar` + `CandleChartHost` (OHLCV), URL `interval`/`limit`, poll + visibility pause.
