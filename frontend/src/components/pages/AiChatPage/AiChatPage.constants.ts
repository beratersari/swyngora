/** Max characters accepted in the composer (server also validates non-empty). */
export const MAX_MESSAGE_LENGTH = 4000;

/** Placeholder suggestion chips for empty state. */
export const SUGGESTION_KEYS = [
  'suggestions.btcRsi',
  'suggestions.ethPrice',
  'suggestions.pumps',
] as const;
