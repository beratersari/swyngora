/** Grouped rows requested from the backend (per side). */
export const ORDER_BOOK_LEVELS = 16;

/** Poll while the tab is visible. Backend order-book cache is ~2s. */
export const ORDER_BOOK_POLL_MS = 2_500;
