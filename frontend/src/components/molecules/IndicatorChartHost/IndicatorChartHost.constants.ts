import { semanticColors } from '@/styles/tokens';

export const DEFAULT_HEIGHT = 160;
export const DEFAULT_BANDS = [30, 70];
export const RSI_LINE_COLOR = semanticColors.chart.rsi;
export const BAND_LINE_COLOR = semanticColors.border.strong;
