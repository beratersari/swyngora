import { palette } from '@/styles/tokens';

export const DEFAULT_HEIGHT = 160;
export const DEFAULT_BANDS = [30, 70];
export const RSI_LINE_COLOR = palette.antiFlashWhite;
export const BAND_LINE_COLOR = 'rgba(170, 203, 196, 0.55)'; // pistachio soft
