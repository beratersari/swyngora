/**
 * React Native native entry (Android/iOS).
 * Browser entry is src/index.web.tsx via Vite (`npm run web`).
 *
 * Full native projects (android/, ios/) are not generated in this web-first init.
 * When native is added, register with:
 *   import { AppRegistry } from 'react-native';
 *   import App from './src/app/App';
 *   AppRegistry.registerComponent('Swyngora', () => App);
 */
console.warn(
  '[swyngora-mobile] Native entry is a placeholder. Run: npm run web  (Chrome at http://localhost:5180)',
);
