# Leave empty for same-origin + Vite proxy (recommended for Chrome/WSL).
# Set only when the browser can reach the API host directly.
# VITE_API_BASE_URL=http://127.0.0.1:8080

# Proxy target used by Vite only (server-side).
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:8080
