/** AI chat session / UI constants (mobile). */

export const AI_SESSION_STORAGE_KEY = 'swyngora.mobile.aiSessionId.v1';
export const AI_SESSION_ID_PREFIX = 'mobile-ai-';

/** Soft cap for in-memory transcript rendering (oldest dropped). */
export const MAX_CHAT_MESSAGES = 100;

/** Default disclaimer when API omits `note`. */
export const AI_CHAT_DISCLAIMER =
  'Informational only — not financial advice. Market numbers should come from tools.';
