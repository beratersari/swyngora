# libs

Non-UI shared code: `api`, `hooks`, `utils`, `types`, **`i18n`**.

Must not import `modules/`, `app/`, or `components/`.

Localization: see `i18n/README.md` (i18next + en/tr catalogs, extend via config + resources).
