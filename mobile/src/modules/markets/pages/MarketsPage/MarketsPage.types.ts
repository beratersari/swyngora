/** Init stub only — no items/sort until mobile-markets epic. */
export type MarketsPageViewModel = {
  title: string;
  subtitle: string;
  isPlaceholder: true;
};

export type MarketsPageProps = {
  viewModel?: MarketsPageViewModel;
};
