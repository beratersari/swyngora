# app

- `App.tsx` — root component
- `providers.tsx` — Redux, SafeArea, GestureHandler, Navigation
- `navigation/` — React Navigation shell

Pages are **not** here; they live under `src/modules/*/pages/`.
